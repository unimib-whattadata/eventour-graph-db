# Curation

This folder contains the auditable Wikidata curation workflow:

- candidate extraction artifacts;
- property catalog and cleanup policy;
- evidence cards;
- prompt template and structured output schema;
- raw model outputs;
- human annotation and adjudication files;
- final Wikidata place decisions.

The LLM is not used as a factual source. It only classifies released evidence cards.


# Application Code

The canonical resource repository links to companion application repositories:

- Eventour UI: `https://github.com/unimib-whattadata/eventour-ui`
- Eventour GraphDB setup: `https://github.com/unimib-whattadata/eventour-graph-db`
- Eventour itinerary generation: `app/itinerary-generation/`

If the UI or GraphDB setup are kept separately, add them as Git submodules or update `app/ui/` and `app/graphdb/` with direct links.

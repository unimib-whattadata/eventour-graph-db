# SHACL Shapes

The SHACL shapes validate geometry coverage, WKT literals, provenance links, source-record structure, Wikidata identity links, role assignment, NIL assignment, and controlled-vocabulary membership.

The current release provides a consolidated `eventour-shapes.ttl`. Split files are included as named copies to match the publication package structure.


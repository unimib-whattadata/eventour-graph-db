# Metadata

This folder contains VoID/DCAT and release metadata for Eventour.

The current metadata file combines VoID and DCAT descriptions. It records graph statistics, source datasets, distributions, vocabularies, themes, and release-level metadata.


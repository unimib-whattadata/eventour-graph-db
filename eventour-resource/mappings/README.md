# Mappings

This folder contains mapping and policy files used to transform municipal and Wikidata sources into Eventour RDF.

- `municipal/`: YAML mappings for official Comune di Milano datasets.
- `wikidata/`: role, category, and NIL-assignment policies for Wikidata-derived semantic places.

